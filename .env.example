PORT=8006
FRONTEND_PORT=3000
NEXT_PUBLIC_API_BASE=http://localhost:8006/api
# Optional: use only when server-side Next.js rewrite target differs.
NEXT_SERVER_API_BASE=
# Optional: comma-separated origins for Next.js dev proxy allowances.
ALLOWED_DEV_ORIGINS=
